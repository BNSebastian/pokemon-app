export interface Pokemon {
  id: number;
  name: string;
  birthDate: string;
  // type: string;
  // isCool: boolean;
  // isStylish: boolean;
}

export interface PokemonType {
  id: number;
  name: string;
}
